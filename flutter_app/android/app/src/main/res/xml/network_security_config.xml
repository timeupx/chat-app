<?xml version="1.0" encoding="utf-8"?>
<!--
  Android 9 (API 28) and above block plaintext HTTP by default. Our backend
  runs on plain http:// for local development, so we explicitly allow
  cleartext traffic for the dev hosts only - not globally - to avoid
  weakening the app's network security for production endpoints.

  If you test on a physical device with your computer's LAN IP (see
  flutter_app/.env), add that IP as another <domain> entry below.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
