<?xml version="1.0" encoding="utf-8"?>
<!--
  Android 9+ blocks plaintext HTTP by default. Local backend uses http://,
  so cleartext is allowed for emulator + common LAN hosts.

  If your Mac LAN IP changes, update BASE_URL in mobile/.env AND the
  matching <domain> below (or keep the debug cleartext override).
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <!-- Current Mac LAN IP (same Wi‑Fi as the phone) -->
        <domain includeSubdomains="false">192.168.10.210</domain>
        <!-- Previous LAN IP kept so old installs still work briefly -->
        <domain includeSubdomains="false">192.168.0.110</domain>
    </domain-config>
</network-security-config>
